global.appName = 'Practices Application'

const addFn = (x, y ) => x + y

const subFn = (x, y ) => x - y

const  multiFn = (x, y) => x * y

const divideFn = (x, y) => x / y


module.exports = {
    addFn,
    subFn,
    multiFn,
    divideFn
}

